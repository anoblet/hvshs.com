---
import Layout from '../layouts/default.astro';
import { getImage } from 'astro:assets';
import aqaraLogo from '@assets/logos/aqara.svg';
import bluetoothLogo from '@assets/logos/bluetooth.svg';
import inovelliLogo from '@assets/logos/inovelli.png';
import philipsHueLogo from '@assets/logos/philips-hue.svg';
import tpLinkLogo from '@assets/logos/tp-link.png';
import thirdRealityLogo from '@assets/logos/third-reality.svg';
import wifiLogo from '@assets/logos/wifi.svg';
import zWaveLogo from '@assets/logos/z-wave.svg';
import zigbeeLogo from '@assets/logos/zigbee.svg';
import catskills from '@assets/images/hudson-valley/catskills-shawangunk.jpg';
import walkway from '@assets/images/hudson-valley/walkway-foggy.jpg';
import midHudson from '@assets/images/hudson-valley/mid-hudson-bridge.jpg';
import blithewood from '@assets/images/hudson-valley/hudson-catskills-blithewood.jpg';
import lighthouse from '@assets/images/hudson-valley/saugerties-lighthouse.jpg';
import shawangunk from '@assets/images/hudson-valley/shawangunk-verkeerderkill.jpg';

const heroSlides = await Promise.all(
  [
    {
      src: catskills,
      alt: 'Catskill Mountains panorama seen from the Shawangunk Ridge near Kerhonkson',
    },
    {
      src: walkway,
      alt: 'Walkway Over the Hudson railroad bridge spanning the Hudson River between Poughkeepsie and Highland',
    },
    {
      src: midHudson,
      alt: 'Mid-Hudson Bridge and Poughkeepsie viewed from Franny Reese State Park in Highland',
    },
    {
      src: blithewood,
      alt: 'Hudson River and the Catskill Escarpment viewed from North Germantown',
    },
    {
      src: lighthouse,
      alt: 'Saugerties Lighthouse on the Hudson River',
    },
    {
      src: shawangunk,
      alt: 'Shawangunk Ridge seen from Bear Hill Preserve near Cragsmoor in autumn',
    },
  ].map(async ({ src, alt }) => {
    const optimized = await getImage({
      src,
      widths: [640, 960, 1280, 1920],
      sizes: '100vw',
      format: 'webp',
    });
    return {
      src: optimized.src,
      srcset: optimized.srcSet.attribute,
      alt,
    };
  })
);

const heroSlidesJson = JSON.stringify(heroSlides);

const logos = [
  {
    src: 'https://cdn.shopify.com/s/files/1/1354/7835/files/logoSvg.svg?12380900890489450926',
    alt: 'August',
    href: 'https://august.com/',
    keepColors: true,
  },
  { src: aqaraLogo.src, alt: 'Aqara', href: 'https://www.aqara.com/' },
  {
    src: bluetoothLogo.src,
    alt: 'Bluetooth',
    href: 'https://www.bluetooth.com/',
    keepColors: true,
  },
  {
    src: 'https://upload.wikimedia.org/wikipedia/commons/a/ab/New_Home_Assistant_logo.svg',
    alt: 'Home Assistant',
    href: 'https://www.home-assistant.io/',
    keepColors: true,
  },
  {
    src: inovelliLogo.src,
    alt: 'Inovelli',
    href: 'https://www.inovelli.com/',
    keepColors: true,
  },
  {
    src: philipsHueLogo.src,
    alt: 'Philips Hue',
    href: 'https://www.philips-hue.com/',
    keepColors: true,
  },
  {
    src: tpLinkLogo.src,
    alt: 'TP-Link',
    href: 'https://www.tp-link.com/',
    keepColors: true,
  },
  {
    src: thirdRealityLogo.src,
    alt: 'Third Reality',
    href: 'https://thirdreality.com/',
  },
  {
    src: wifiLogo.src,
    alt: 'Wi-Fi',
    href: 'https://www.wi-fi.org/',
    keepColors: true,
  },
  {
    src: zWaveLogo.src,
    alt: 'Z-Wave',
    href: 'https://z-wavealliance.org/',
    keepColors: true,
  },
  {
    src: zigbeeLogo.src,
    alt: 'Zigbee',
    href: 'https://csa-iot.org/all-solutions/zigbee/',
  },
];
---

<Layout
  title="Hudson Valley Smart Home Solutions"
  description="Hudson Valley Smart Home Solutions - subscription-based smart home design, installation, maintenance, and support for lighting, climate, security, and entertainment automation."
  fullBleed
>
  <hero-component images={heroSlidesJson}></hero-component>
  <section
    class="logo-carousel"
    aria-label="Smart home technologies we support"
  >
    <button
      class="logo-carousel__nav logo-carousel__nav--prev"
      aria-label="Previous logos"
    >
      <svg viewBox="0 -960 960 960" aria-hidden="true">
        <path
          d="m313-440 224 224-57 56-320-320 320-320 57 56-224 224h487v80H313Z"
        ></path>
      </svg>
    </button>
    <div class="logo-carousel__track">
      {
        [0, 1].map(group => (
          <div class="logo-carousel__group" aria-hidden={group === 1}>
            {logos.map(({ src, alt, href, keepColors }) => (
              <a
                href={href}
                target="_blank"
                rel="noreferrer"
                tabindex={group === 1 ? -1 : undefined}
              >
                <img
                  src={src}
                  alt={alt}
                  data-logo={alt}
                  class:list={[{ 'logo-carousel__img--color': keepColors }]}
                />
              </a>
            ))}
          </div>
        ))
      }
    </div>
    <button
      class="logo-carousel__nav logo-carousel__nav--next"
      aria-label="Next logos"
    >
      <svg viewBox="0 -960 960 960" aria-hidden="true">
        <path
          d="M647-440H160v-80h487L423-744l57-56 320 320-320 320-57-56 224-224Z"
        ></path>
      </svg>
    </button>
  </section>
</Layout>

<style>
  .logo-carousel {
    position: relative;
    width: 100%;
    overflow: hidden;
    background: #fff;
  }

  .logo-carousel:hover .logo-carousel__track {
    animation-play-state: paused;
  }

  .logo-carousel__nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 2;
    width: 2.5rem;
    height: 2.5rem;
    border: none;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.9);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s;
  }

  .logo-carousel__nav:hover {
    background: #fff;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  }

  .logo-carousel__nav svg {
    width: 1.5rem;
    height: 1.5rem;
    fill: #333;
  }

  .logo-carousel__nav--prev {
    left: 1rem;
  }

  .logo-carousel__nav--next {
    right: 1rem;
  }

  .logo-carousel__track {
    display: flex;
    align-items: center;
    width: max-content;
    animation: logo-carousel-scroll 28s linear infinite reverse;
  }

  .logo-carousel__group {
    display: flex;
    align-items: center;
    gap: 2rem;
    padding: 1.5rem 2rem;
  }

  .logo-carousel a {
    display: grid;
    width: 8rem;
    height: 4.5rem;
    flex: 0 0 8rem;
    place-items: center;
    border-radius: 0.25rem;
    background: #f5f5f5;
  }

  .logo-carousel img {
    display: block;
    width: 6rem;
    height: 3rem;
    object-fit: contain;
    filter: brightness(0);
  }

  .logo-carousel img.logo-carousel__img--color,
  .logo-carousel img[data-logo='August'],
  .logo-carousel img[data-logo='Bluetooth'],
  .logo-carousel img[data-logo='Inovelli'],
  .logo-carousel img[data-logo='Philips Hue'],
  .logo-carousel img[data-logo='TP-Link'],
  .logo-carousel img[data-logo='Wi-Fi'],
  .logo-carousel img[data-logo='Z-Wave'] {
    filter: none;
  }

  .logo-carousel::before,
  .logo-carousel::after {
    position: absolute;
    z-index: 1;
    top: 0;
    bottom: 0;
    width: 4rem;
    content: '';
    pointer-events: none;
  }

  .logo-carousel::before {
    left: 0;
    background: linear-gradient(to right, #fff, rgb(255 255 255 / 0%));
  }

  .logo-carousel::after {
    right: 0;
    background: linear-gradient(to left, #fff, rgb(255 255 255 / 0%));
  }

  @keyframes logo-carousel-scroll {
    from {
      transform: translateX(0);
    }

    to {
      transform: translateX(-50%);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .logo-carousel__track {
      animation: none;
    }
  }

  @media (max-width: 768px) {
    .logo-carousel a {
      width: 6rem;
      height: 3.5rem;
      flex: 0 0 6rem;
    }

    .logo-carousel img {
      width: 4.5rem;
      height: 2.25rem;
    }

    .logo-carousel__group {
      gap: 1.5rem;
      padding: 1rem 1.5rem;
    }

    .logo-carousel__nav {
      width: 2rem;
      height: 2rem;
    }

    .logo-carousel__nav svg {
      width: 1.25rem;
      height: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .logo-carousel a {
      width: 5rem;
      height: 3rem;
      flex: 0 0 5rem;
    }

    .logo-carousel img {
      width: 3.75rem;
      height: 1.875rem;
    }

    .logo-carousel__group {
      gap: 1rem;
      padding: 0.75rem 1rem;
    }
  }
</style>
<script>
  import '@components/button';
  import '@components/hero';

  // Logo carousel navigation
  const carousel = document.querySelector('.logo-carousel');
  const track = carousel?.querySelector('.logo-carousel__track');
  const prevBtn = carousel?.querySelector('.logo-carousel__nav--prev');
  const nextBtn = carousel?.querySelector('.logo-carousel__nav--next');

  if (track && prevBtn && nextBtn) {
    const getScrollAmount = () => {
      const logo = track.querySelector('a');
      if (!logo) return 200;
      const style = getComputedStyle(logo);
      const width = logo.offsetWidth;
      const marginRight = parseInt(style.marginRight) || 0;
      return width + marginRight;
    };

    let currentOffset = 0;
    const maxOffset = track.scrollWidth / 2;

    const updatePosition = () => {
      track.style.transform = `translateX(${currentOffset}px)`;
    };

    prevBtn.addEventListener('click', () => {
      // Pause animation
      track.style.animationPlayState = 'paused';

      // If animation is running, get current position
      if (!currentOffset) {
        const computed = getComputedStyle(track);
        const matrix = new DOMMatrix(computed.transform);
        currentOffset = matrix.m41;
      }

      currentOffset += getScrollAmount();
      if (currentOffset > 0) currentOffset = 0;
      updatePosition();
    });

    nextBtn.addEventListener('click', () => {
      // Pause animation
      track.style.animationPlayState = 'paused';

      // If animation is running, get current position
      if (!currentOffset) {
        const computed = getComputedStyle(track);
        const matrix = new DOMMatrix(computed.transform);
        currentOffset = matrix.m41;
      }

      currentOffset -= getScrollAmount();
      if (currentOffset < -maxOffset)
        currentOffset = -maxOffset + getScrollAmount();
      updatePosition();
    });
  }
</script>
