<copyright-component class="align-items-center flex"></copyright-component>
<script>
  import { Base } from '@components/base';
  import { css, html } from 'lit';
  import { customElement } from 'lit/decorators.js';

  @customElement('copyright-component')
  class CopyrightComponent extends Base {
    static styles = [
      ...super.styles,
      css`
        :host {
          --icon-color: var(--color-rose-400);
          --icon-size: 1.5rem;

          align-items: center;
          flex-direction: column;
          gap: 0.25rem;
          justify-content: center;
          padding: var(--padding-0-2-5);
          text-align: center;

          > * {
            display: inline-flex;
          }
        }
      `,
    ];
    render() {
      return html`
        <span
          >&copy; ${new Date().getFullYear()} Hudson Valley Smart Home
          Services</span
        >
      `;
    }
  }
</script>
